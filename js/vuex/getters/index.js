export default {
	leftMatched (st) {
		return st.leftMatched;
	},
	highestSpeed (st) {
		return st.highestSpeed;
	},
	status (st) {
		return st.status;
	},
	cards (st) {
		return st.cards;
	},
	elapsedMs (st) {
		return st.elapsedMs;
	}
}